
let row = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]
    let row=" "
    for(let i=N-1; i>=0; i--){
        row += matrix[i][0]+" ";
    }
    for(let j=1; j<=N-1; j++){
        row += matrix[0][j]+" ";
    }
    for(let i=1; i<=N-1; i++){
        row += matrix[i][N-1]+" ";
    }
    for(let j=N-2; j>0; j--){
        row += matrix[N-1][i]+" ";
    }
    console.log(row)

